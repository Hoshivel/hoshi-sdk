module github.com/hoshivel/hoshi-sdk/go

go 1.25.0

// This module has no require block, and is not going to grow one. Everything
// here is standard library only — a service that adopts one of these packages
// should not inherit a dependency tree along with it.
